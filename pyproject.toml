[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cpm-install"
version = "1.0.0"
description = "Installer for claude-project-manager plugin marketplace"
requires-python = ">=3.12"
dependencies = ["textual>=0.80", "pyyaml>=6.0", "httpx>=0.28", "packaging>=24.0"]

[project.scripts]
cpm-install = "installer.main:main"

[project.optional-dependencies]
plugins = [
    "mcp>=1.2.0",
    "pyyaml>=6.0",
    "gitpython>=3.1",
    "httpx>=0.28",
    "claude-hook-transport",
]

[tool.hatch.build.targets.wheel]
packages = ["installer"]
force-include = { "installer/defaults.yaml" = "installer/defaults.yaml", ".claude-plugin/marketplace.json" = "installer/marketplace.json" }

[dependency-groups]
test = ["pytest>=8.0", "pytest-cov>=7.0", "pytest-mock>=3.14", "pytest-asyncio>=0.24", "textual[dev]>=0.80", "pytest-textual-snapshot>=1.0", "httpx>=0.28", "respx>=0.22"]

[tool.pytest.ini_options]
markers = ["slow: tests that build/install packages (may take >10s)"]

[tool.ruff.lint.per-file-ignores]
"installer/tests/**" = ["S"]
"tests/**" = ["S"]
"scripts/tests/**" = ["S"]

[tool.uv.sources]
claude-hook-transport = { path = "plugins/_shared" }
