{
  "mcpServers": {
    "worktree": {
      "command": "uv",
      "args": [
        "--directory", "${CLAUDE_PLUGIN_ROOT}/server",
        "run", "worktree-server"
      ],
      "env": {
        "WORKTREE_CONFIG": "~/.claude/worktree.yaml"
      },
      "timeout": 120
    }
  }
}
