{
  "mcpServers": {
    "router": {
      "command": "bash",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/start.sh",
        "${CLAUDE_PLUGIN_ROOT}/server",
        "router-server"
      ],
      "timeout": 120
    }
  }
}
